{
  "name": "life-structure",
  "version": "0.4.1",
  "description": "A calm, full-page activity heatmap for building daily momentum in Obsidian.",
  "private": true,
  "license": "MIT",
  "repository": "github:0libote/Life-Structure",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "esbuild src/main.js --bundle --external:obsidian --format=cjs --outfile=main.js",
    "dev": "bun run build --watch",
    "test": "bun test",
    "check": "bun run build && bun test"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
